# AGENTS.md

## 最高优先级硬规则

在执行任何修改前，先判断目标文件类型：

- `.cs`：允许按需求修改。
- `.md`：只有用户明确要求写文档时才允许修改。
- `.prefab` / `.unity` / `.asset` / `.meta` / `.mat` / `.controller` / `.anim` / `.playable`：默认禁止修改。

如果任务看起来必须修改 Unity 资产文件：
1. 停止执行。
2. 明确告诉用户需要改哪个资产文件、为什么必须改。
3. 等用户明确授权后才能改。

禁止用“避免 Missing Script”“保持引用完整”“顺手清理”“验证需要”作为擅自修改资产的理由。

删除或移动 `.cs` 文件时，`.cs.meta` 是否一起处理必须单独询问用户。

## 沟通规则

- 说话短、狠、准；结论先行，理由后补。
- 不要客服腔、三明治结构、复述用户问题、空话套话。
- 不确定就说“不确定”，不要装懂，也不要盲目猜用户需求。
- 用户可能自己都不知道自己想要什么，需要你多加引导，多问问题。
- 用户提出疑问时，只分析，不改文件。
- 需求、范围、目标不清楚时先问，不要擅自开工。

## 项目概况

DreamSenker 是 Unity 2022.3.62f2c1 的 2D 动作游戏项目，使用 URP 14.0.12。

主要结构：

- `Assets/Framework/`：基础设施，包含 `BaseManager<T>`、资源管理、UI 管理、事件中心、对象池、数据持久化。
- `Assets/PlayArk/`：自研编辑器工具，核心是 `GraphCore`，其上有 `StateMachine` 和 `DialogueSystem`。
- `Assets/Scripts/`：游戏逻辑，包含角色、怪物、运行时数据、场景与地图逻辑。

主要依赖：Cinemachine、TextMeshPro、DOTween。

## 当前架构判断

- 项目正在从“能跑”走向“可维护、可展示”的重构阶段。
- 给架构建议时，优先给长期正确的方案，同时说明复杂度、学习成本、前置知识、设计模式和过度设计风险。
- 至少给一个推荐方案和一个更简单的替代方案，明确当前项目更适合哪一个。
- 不要用初学者口吻解释 Unity 基础，但关键架构取舍要讲清楚。

## 代码约定
- 重构或移动代码时必须保留原注释；只有注释错误、过期或冲突时允许在原注释下添加新注释，具体要求为：
原注释 --- 标注出原注释的状态 错误、过期或冲突
新注释 --- 另起一行在原注释下面写新注释

- 新增或修改代码后，再扫描一遍本次改动的代码，为代码添加适量的中文注释
- 每一个新增或修改的功能函数都标注出具体功能，同一作用的字段也标注出简要说明
- 如果是一段结构性功能性很具体的语句段，也标注出合适的简要注释

## Git 与验证
- 任何不可逆的Git操作必须要请示用户。
- 涉及 Unity 代码改动时，默认只做轻量验证：用 `rg` 查关键引用，用 `git diff --check` 查 diff 格式，并检查 Unity Console Error。
- 项目里有明确测试时，才跑 UnityMCP `tests_run`。
- 不默认跑 `dotnet build Assembly-CSharp.csproj`；只有用户明确要求时，才做额外编译或构建验证。
